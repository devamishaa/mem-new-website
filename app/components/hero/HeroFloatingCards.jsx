"use client";

import React from "react";

const HeroFloatingCards = ({ model }) => {
  // Placeholder component - can be implemented later with actual floating cards
  return (
    <div className="absolute inset-0 pointer-events-none">
      {/* Floating cards will be implemented here */}
      {/* This is a placeholder to prevent the "Element type is invalid" error */}
    </div>
  );
};

export default HeroFloatingCards;
